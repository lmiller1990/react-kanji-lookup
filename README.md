An app to lookup kanji by radical.

# install dependencies
```yarn install```

# to run
```yarn start```

Select a radical by clicking, and in the console, all characters using that radical will be output.
